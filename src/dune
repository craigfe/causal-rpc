(library
	(name trace_rpc)
	(public_name trace_rpc)
	(flags (-thread))
	(preprocess (pps ppx_deriving.std))
	(libraries
		alcotest
		digestif.c
		cmdliner
		fmt.cli
		fmt.tty
		irmin
		irmin-unix
		logs.cli
		logs.fmt
		lwt
		ocamlgraph))
